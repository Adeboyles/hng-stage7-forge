# Forge environment variables
#
# Keep runtime platform settings in config.yaml.
# Use environment variables only for environment-specific concerns such as
# locating the config file or overriding secrets in deployment.

# Path to the Forge config file
# Forge environment variables
#
# Keep runtime platform settings in config.yaml.
# Use environment variables only for environment-specific concerns such as
# locating the config file or overriding secrets in deployment.

# Path to the Forge config file
FORGE_CONFIG=/app/config.yaml

